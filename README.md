Simple tg casino bot,dont have a database rn mb add it later.

Library: Built with pyTelegramBotAPI.

Token: Replace the token in TeleBot("...") with your own.

Note: Real-time spin animation, but no database (global balance only).